<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Register Form</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <style>
      .glass {
        background: rgba(255, 255, 255, 0.1);
        border-radius: 16px;
        box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
        backdrop-filter: blur(10px);
        -webkit-backdrop-filter: blur(10px);
        border: 1px solid rgba(255, 255, 255, 0.2);
      }
      /* body {
        background-color: #000;
      } */
    </style>
  </head>
  <body class="flex items-center justify-center min-h-screen text-white bg-gray-900">
    <div class="w-full max-w-md p-4">
      <form
        class="glass p-8 rounded-lg shadow-lg"
        action="/register"
        method="post"
      >
        <h2 class="text-3xl font-bold mb-6 text-center text-yellow-500">Register</h2>

        <div class="mb-4">
          <label class="block text-sm font-bold mb-2 text-white" for="name">Name</label>
          <input
            class="w-full px-3 py-2 text-black rounded-lg focus:outline-none focus:ring-2 focus:ring-yellow-500"
            type="text"
            id="name"
            name="name"
            required
          />
        </div>

        <div class="mb-4">
          <label class="block text-sm font-bold mb-2 text-white" for="username">Username</label>
          <input
            class="w-full px-3 py-2 text-black rounded-lg focus:outline-none focus:ring-2 focus:ring-yellow-500"
            type="text"
            id="username"
            name="username"
            required
          />
        </div>

        <div class="mb-4">
          <label class="block text-sm font-bold mb-2 text-white" for="password">Password</label>
          <input
            class="w-full px-3 py-2 text-black rounded-lg focus:outline-none focus:ring-2 focus:ring-yellow-500"
            type="password"
            id="password"
            name="password"
            required
          />
        </div>

        <div class="mb-4">
          <label class="block text-sm font-bold mb-2 text-white" for="email">Email</label>
          <input
            class="w-full px-3 py-2 text-black rounded-lg focus:outline-none focus:ring-2 focus:ring-yellow-500"
            type="email"
            id="email"
            name="email"
            required
          />
        </div>

        <div class="mb-4">
          <label class="block text-sm font-bold mb-2 text-white" for="age">Age</label>
          <input
            class="w-full px-3 py-2 text-black rounded-lg focus:outline-none focus:ring-2 focus:ring-yellow-500"
            type="number"
            id="age"
            name="age"
            required
          />
        </div>

        <div class="flex items-center justify-between">
          <button
            class="bg-yellow-500 text-black font-bold py-2 px-4 rounded-lg hover:bg-yellow-600 focus:outline-none focus:ring-2 focus:ring-yellow-500"
            type="submit"
          >
            Submit
          </button>
        </div>

        <div class="mt-4 text-center">
          <p>Already have an account? <a href="/login" class="text-yellow-500 font-bold">Login</a></p>
        </div>
      </form>
    </div>
  </body>
</html>
